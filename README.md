# Pendragon

Self-contained HTML templates:

- `rule-cheat-sheet-template.html` - build and print quick-reference rule cards.
- `character-sheet-template.html` - fillable printable character sheet with local persistence.

Both files keep CSS/JS inline and can run locally with no external dependencies.

Main entry page:

- `index.html` - responsive landing page with navigation to both templates.

